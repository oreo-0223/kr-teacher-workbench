# 科任教师工作台 — 完善记录（2026-08-10）

## 本次做了什么

通读并真机验证了「科任教师工作台」全栈项目（前端 HTML/CSS/JS 8 模块 + Node.js 22 / node:sqlite 后端 + 部署脚本），发现并修复了一个**会导致真实数据丢失的关键 bug**。

## 关键修复：备份导入 / 删除回滚失效

**文件**：`server/database.js` → `putRaw()` 通用表分支

**根因**：`putRaw` 在传入 `id` 时执行的是 `UPDATE ... WHERE id=?`。
当目标行**已经不存在**（被删除、或被导入时的 `DELETE` 清空）时，`UPDATE` 命中 0 行、静默失效。这直接导致两个用户可见的功能坏掉：

1. **备份恢复（设置 → 导入备份）完全失效**：`importAll` 先 `DELETE` 全表再用 `putRaw` 逐条写回，带 id 的 `putRaw` 变成 UPDATE 空表 → 把所有数据清空且一条都写不回去。
2. **回滚「删除」操作失效**：在「操作日志」里对一条"删除"记录点回滚，本应恢复该记录，但 `rollback(delete)` 走 `putRaw` → UPDATE 已删除的行 → 恢复不了。

**修复**：把通用表的 `UPDATE` 改为 `INSERT OR REPLACE`。

```js
// 修复前
if (id) {
  db.prepare(`UPDATE ${table} SET class_name = ?, data = ? WHERE id = ?`).run(className, json, id);
  return id;
}
// 修复后
if (id) {
  db.prepare(`INSERT OR REPLACE INTO ${table} (id, class_name, data) VALUES (?, ?, ?)`).run(id, className, json);
  return id;
}
```

效果：行存在时等价 UPDATE（编辑/更新场景不变），行已删除时重新插入（导入恢复、删除回滚场景修复）。

## 验证

- 写了隔离的单测 `test_db.cjs`（在临时库 `require('./server/database.js')` 跑，不触碰真实数据），覆盖：账号校验、学生/考勤 CRUD、成绩统计、操作日志、**删除回滚恢复**、**导入导出往返**。
- 修复前：删除回滚恢复失败、导入后数据条数变为 0。
- 修复后：**14 项断言全部通过**，导入完整恢复数据、删除回滚成功恢复记录。
- 测试产物与临时库已清理；真实数据库 `server/data/teacher_data.db`（admin / 测试老师 / 四年级1班、五年级3班）未被修改。
- 已重启服务（端口 3000），确认用修复后的代码正常运行。

## 完善方向一：首页「今日待处理」概览 ✅

**文件**：`js/workbench.js`（新增 `renderToday()` + 跳转辅助 `goSchedule/loadHwAssign/loadHwCollect/loadAttendance/loadAbnormal`）、`css/style.css`（`.today-panel` 系列样式）

首页顶部新增一张渐变面板，自动聚合多个数据源给出当天提醒：
- **今日课程**：工作日按 `schedule` 当周几（day）列出节次；周末提示无课
- **今日截止作业**：`homeworkAssign.deadline` 命中今天的条目数 + 内容摘要
- **最近一次收缴**：未交 / 未订正学生名单（来自最新 `homeworkCollect`，最多展示 10 人）
- **今日考勤**：是否已记录（命中 `attendance` 当天有数据则显示已记录，否则红色提醒）
- **近 7 天异常**：`abnormalLog` 近一周条目，红色待跟进
- 每条提醒带「一键跳转」按钮，直达对应功能页

## 完善方向二：花名册 CSV 批量导入 / 导出 ✅

**文件**：`js/roster.js`、沿用 `css/style.css` 既有按钮样式

- **导入 CSV**：弹窗支持「选择文件」或「直接粘贴」；自动跳过含"姓名/学号"的表头；列顺序 `学号,姓名,性别,班级,备注`；未填班级的归入当前班级（全部班级模式强制要求提供"班级"列）；逐行 `DB.op('students','create')`，导入后自动刷新列表并告知导入/跳过数。
- **导出 CSV**：导出当前班级（全部班级模式导出全部）学生为 `学号,姓名,性别,班级,备注`，带 BOM 头，文件名含班级与日期。

## 完善方向三：成绩分析增强 ✅

**文件**：`js/grades.js`（新增 `trendCard()` 手绘内联 SVG 折线图、`PALETTE` 配色、`exportGradesCsv()`）、`css/style.css`（`.chart-card` / `.trend-svg` / `.trend-legend`）

- **单班均分趋势**：本班考试 ≥2 次时，在列表上方绘制「历次考试均分趋势」折线图（按日期升序，带坐标轴/网格/数据点）。
- **各班均分趋势对比**：全部班级模式下，绘制「各班均分趋势对比」多系列折线图（每班一色，缺失考试用 null 断点）。
- **导出 CSV**：当前班级导出「学生 × 考试」成绩矩阵（列=各次考试名+日期）；全部班级导出扁平明细 `班级,考试,日期,姓名,分数,备注`。带 BOM，文件名含范围与日期。

## 验证

- 5 个改动文件 `node --check` 语法全过（grades/roster/workbench/database/server）。
- 服务端已重启（端口 3000），首页与 `/api/auth/check` 均返回 200；元数据含四年级1班、五年级3班。
- 通过 grep 核对：新代码引用的 `App.*`（`activeClass/isAll/classList/classSubject/studentsOf/pad/openDrawer…`）与 `DB.getAll` 各 store 键（`schedule/homeworkAssign/homeworkCollect/attendance/abnormalLog/grades/students`）与现有代码完全一致，无悬空引用。
- 对 `trendCard` 的 SVG 坐标数学做了边界测试（多次考试 / 含 null 断点 / 全部同分 / 仅 1 次考试），均**无 NaN/undefined**，输出合法路径。

## 安全加固：口令哈希升级为 scrypt ✅

**文件**：`server/database.js`

**动机**：原 `hashPwd` 是「固定盐的字符串弱哈希」（等效明文可轻易反推），不适合作为口令存储，外网部署时风险尤其大。

**方案**：改用 Node 22 内置 `crypto.scryptSync`（**零外部依赖**，与项目「node:sqlite 单文件、无第三方包」的一致性相符；argon2 需额外 native 依赖，故不选）。
- 存储格式自描述：`scrypt$N:r:p$<saltB64>$<hashB64>`（盐随机 16 字节、keylen 64、N=16384/r=8/p=1）。
- 校验用 `crypto.timingSafeEqual` 做常数时间比较，防时序侧信道。
- 影响点全部覆盖：`addAccount` / `updateAccount`（改密）/ `setMeta`（设置页改访问口令）/ `verifyAccount`。

**向后兼容（无数据迁移、不丢账号）**：
- 历史账号仍存旧弱哈希；`verifyAccount` 检测非 `scrypt$` 前缀即走旧逻辑比对。
- 历史账号**首次成功登录时就地升级**：把 `pwd_hash` 列与 `data.pwdHash` 同时改写为 scrypt。此后即走强哈希路径。
- 因此现有 `admin` 账号无需重置口令，下次登录即自动升级。

**验证**：在临时库 `DB_PATH` 指向的测试库上跑集成测试（不触碰真实 `teacher_data.db`），11 项断言全过：新建账号存 scrypt$、正确口令成功/错误口令拒绝、历史弱哈希可兼容登录且登录后升级为 scrypt$、改密后旧口令失效、跨账号不串号。测试库已清理；真实库未被读取或修改。已重启服务（端口 3000）确认新代码正常运行。

> 注：口令存储已升级为 scrypt（Node 内置），本地单机与外网部署均适用，无需再升级 bcrypt/argon2。

## 小迭代：课堂点名界面优化（2026-08-10 补）

**文件**：`js/classroom.js` + `css/style.css`

**需求**：大屏场景下，随机点名按钮要够大、位置要够低；新增「不重复点名」选项。

**改动**：
- 把控制栏拆分为顶部（日期/节次）和底部（按钮区）。
- 底部控制栏 `.rc-bottombar` 始终贴在视口底部（`margin-top:auto` + 父容器 flex column），方便站在大屏前的老师点击。
- 随机点名按钮升级为 `.rc-big-btn`：字号 18px、最小高度 60px、加大内边距，视觉突出。
- 新增 `.rc-check` 大复选框（26×26px）和「不重复点名」标签，与随机按钮并列。
- 实现「不重复点名」逻辑：
  - 勾选后，每次随机只在未点过的学生中抽取；
  - 全部学生都被点过后，自动清空已点池并弹出提示，开始新一轮。
  - 重置（切换日期/重新进入点名页）会清空已点池。

**验证**：`js/classroom.js` 语法检查通过；curl 确认服务端返回含新类名的 JS/CSS；静态资源无需重启服务。

## 小迭代：作业收缴支持编辑（2026-08-10 补）

**文件**：`js/workbench.js`

**需求**：登记完的作业收缴状态无法修改，需要支持后续编辑。

**改动**：
- 每条收缴记录卡片右上角增加「编辑」按钮（与「删除」并列）。
- `hwCollectForm()` 改为支持传入已有记录：
  - 打开时标题变为「编辑作业收缴」；
  - 自动回填日期、班级、关联的作业布置、作业内容、以及每位学生的已交/已订正状态；
  - 保存时调用 `DB.op('homeworkCollect', 'update', ...)` 更新原记录。
- 兼容学生已删除的情况：编辑时若某条记录的学生已不在当前班级，会保留为「已删除学生」行，仍可修改其状态，避免历史数据丢失。

**验证**：`js/workbench.js` 语法检查通过；curl 确认服务端已返回含 `编辑作业收缴` / `data-edit` / `homeworkCollect', 'update'` 的新文件；静态资源无需重启。

## 小迭代：课程表合并为单表 + 时段结构（2026-08-10 补）

**文件**：`js/schedule.js` + `css/style.css`

**需求**：老师账号的课程表不要按班级分组，合并成一张；每天的课程按 早读 / 上午1234 / 下午5678 / 晚自习 的结构展示。

**改动**：
- 重写 `schedule.js`：
  - 「全部班级」视图下只渲染**一张合并课表**，不再按班级堆叠多张表。
  - 时段模型改为 10 行：早读(0)、上午1-4、下午5-8、晚自习(9)。历史 `period` 1-8 自动对应到上午/下午，向后兼容。
  - 合并表中每个单元格聚合**所有班级**在该时段的课程，垂直堆叠展示（科目 + 班级标签 + 备注）。
  - 点击合并表单元格 → 弹出「该时段课程管理」弹窗：列出全部课程（可改/删），并可在底部继续添加（可含多个班级）。
  - 单班级视图保持原来的「单格新建/编辑/清除」交互。
- CSS：放宽课表单元格高度（合并多课程可自动展开），新增 `.tt-cell-item` 分隔线。

**验证**：`js/schedule.js` 语法检查通过；curl 确认服务端已返回含 `SLOTS` / `mergedCellModal` / `早读` / `晚自习` 的新文件；静态资源无需重启。

## 小迭代：课表时段按船寮高级中学夏季作息表 + 显示时间（2026-08-11）

**文件**：`js/schedule.js`（时段模型 + 表格列）、`css/style.css`（`.tt-slot-label` / `.tt-slot-time` / `.tt-modal-time`）、`index.html`（版本号 `2026081104` → `2026081105`）

**需求**：之前时段列只显示「早读 / 上午 1 / 下午 5 / 晚自习」等占位文字，没有时间。换成船寮高级中学夏季作息时间表（执行时间 5.1-9.30）的真实节次编号 + 时间。

**改动**：
- `SLOTS` 全部重写：每条加 `time` 字段，标签改成 `早读` / `第1节` … `第8节` / `晚自习`。
  - 时间映射：`早读 6:50-7:20` / `第1节 7:40-8:25` / `第2节 8:55-9:40` / `第3节 9:50-10:35` / `第4节 10:45-11:30` / `第5节 13:50-14:35` / `第6节 14:45-15:30` / `第7节 15:45-16:30` / `第8节 16:40-17:25` / `晚自习 18:30-19:10`。
  - 兼容旧数据：period 1-8 的语义不变，新增 `slotTime(p)` / `slotFull(p)` 辅助。
- `gridHtml()` 时段单元格改为两行：上行节次、下行时间（半透明小字号），保留原早读/上午/下午/晚自习的配色变量。
- `cellClick` / `recordModal` / `mergedCellModal` 的弹窗标题后追加一个米黄色胶囊 `.tt-modal-time` 显示该节的时间。
- 课程表描述区更新为「时段按船寮高级中学夏季作息时间表（执行时间 5.1-9.30）」。
- 课表首列最小宽度从 62px 加到 72px，避免被时间挤窄。

**验证**：`node --check js/schedule.js` 语法通过；版本号升至 `2026081105` 强制浏览器重新加载 schedule.js / style.css；静态资源无需重启服务。

## 小迭代：课表扩展到 16 行，加入非上课时段（2026-08-11）

**文件**：`js/schedule.js`（`SLOTS` 增加 `type` 字段 + `gridHtml` 休息条分支）、`css/style.css`（`.tt-break` / `.tt-break-badge` / `.tt-break-time`）、`index.html`（版本号 `2026081105` → `2026081106`）

**需求**：上一轮只排了上课节次，课间操 / 午静(午睡) / 眼保健操 / 晚间运动这些非上课时段也要出现在课表里。

**改动**：
- `SLOTS` 扩展到 16 行，新增 `type` 字段：`class`（可排课）与 `break`（非上课）。
  - 休息时段条：`课间操 8:25-8:55`（p=101）、`午静(午睡) 12:30-13:35`（p=102）、`眼保健操 15:30-15:35`（p=103）、`晚间运动 20:10-20:40`（p=104）。休息时段编号用 101+/102+/103+/104，与上课 period（0-11）不冲突，旧数据不会污染休息条。
  - 上课节次微调：`晚自习` 改名 `晚读`（p=9，18:30-19:10），新增 `晚自修(1)`（p=10，19:20-20:10）、`晚自修(2)`（p=11，20:40-21:30）两个晚间排课间段。
- `gridHtml()`：`type==='break'` 的行渲染为 `colspan="6"` 的整行灰色时段条（虚线徽章 + 时间），**不可排课、不可点击**；`type==='class'` 保持原有 5 天排课格。
- GROUP_BG 新增 `'break': 'var(--gray-soft)'`（中性灰）；CSS 新增 `.tt-break` 系列样式（虚线白徽章 + 半透明时间）。
- 兼容：旧 `period 1-8` 语义不变；合并视图只对有 `data-merged`/`data-day` 的上课格绑定点击，休息条无属性故天然不可点。

**验证**：`node --check js/schedule.js` 通过；版本号升至 `2026081106` 强制重载；静态资源无需重启。

### 时段可编辑 + 调整为夏季作息（第2轮）

按用户图片里的船寮高级中学夏季作息时间表重排，并把左侧时段改成可编辑状态。

- `SLOTS` 升级为 `DEFAULT_SLOTS`，存于浏览器 `localStorage('tt.slots')`；每项新增稳定 `id` 字段，渲染时读取最新配置。
- 时间按图片调整：早读 6:40-7:20 / 第一节 7:40-8:25 / 第二节 8:35-9:20 / 第三节 9:50-10:35 / 第四节 10:45-11:30 / 眼保健操 11:37-11:45（break，105）/ 午休 12:30-13:35（break，102）/ 第五节 13:20-14:05 / 第六节 14:15-15:00 / 第七节 15:25-16:10 / 第八节 16:20-17:05 / 眼保健操 17:13-17:19（break，106）/ 晚饭 17:30-18:10（break，107）/ 晚读 18:20-19:00 / 晚一 19:10-20:05 / 晚二 20:30-21:20。
- **新增 `slotEditModal(slotId)`**：点击左侧时段进入编辑弹窗，可改「名称 / 时间 / 类型 / 编号」，支持「保存 / 删除 / 恢复默认」三个动作。删除时会把该时段上的排课记录一并清除并提示。
- **新增 `addSlotModal()`**：表头新增「＋ 新增时段」按钮，允许插自定义时段（自动校验编号冲突）；同位置「恢复默认时段」按钮一次性把全部时段重置回默认值。
- `draw()` 给 `td.tt-slot-cell` 绑定点击事件进入编辑；hover 时给单元格加黄底 + 内阴影 + 右上角「编辑」徽章。
- CSS 新增 `.tt-slot-cell` / `.tt-slot-cell:hover::after` 可编辑视觉反馈，以及 `.tt-toolbar` 工具栏样式、`.hint` 提示小字。

**验证**：`node --check js/schedule.js` 通过；版本号升至 `2026081107` 强制重载；静态资源无需重启。

## 小迭代：删两个眼保健操 + 加 loadSlots 迁移自动清缓存（2026-08-11）

**问题**：默认配置删了眼保健操（`eyebc1` 11:37-11:45 / `eyebc2` 17:13-17:19），但浏览器 `localStorage('tt.slots')` 里旧的还在，用户硬刷新后还看得到两条眼保健操，需要手动点「恢复默认时段」才消失——体感像"刷新没生效"。

**修复**：`loadSlots()` 增加迁移逻辑——定义 `REMOVED_SLOT_IDS = {eyebc1, eyebc2}`，加载时过滤掉并把清理后的数组立即写回 localStorage。以后再有"已下线"时段，往这个集合里加 id 即可。

**文件**：`js/schedule.js`（`loadSlots` 增加过滤 + 立即持久化）、`index.html`（版本号 `2026081108` → `2026081109`）

**验证**：`node --check` 通过；硬刷新后两条眼保健操直接消失，无需手动清缓存或点恢复默认。

## 重要改进：时段配置从 localStorage 迁移到数据库 meta.slots（2026-08-11）

**问题**：左侧时段配置（作息表行）之前只存浏览器 `localStorage('tt.slots')`，换设备/清缓存丢失，且各账号互不共享——做不到用户要求的"所有数据进库 + 和其他账号同步"。

**现状核对**：排课记录 `schedule` 表、学生/考勤/成绩等都在后端 SQLite，且多设备多账号共享；唯独时段配置在 localStorage。

**修复**：`js/schedule.js` 的 `loadSlots/saveSlots` 改为读写 `DB.getMeta()/setMeta()` 的 `meta.slots` 字段（meta 是全局配置，天然全校共享、跨设备、跨账号）。`loadSlots` 改为异步，优先读数据库；首次访问时把旧 localStorage 数据迁移进库并清掉本地副本；`REMOVED_SLOT_IDS` 迁移逻辑保留。提示文案改为「时段已存到服务器，全校账号共享、换设备不丢失」。

**后端**：无需改动——`/api/meta` 的 GET/PUT 已存在，setMeta 合并逻辑支持任意字段（slots 数组整体覆盖），隔离测试通过（用临时 DB_DIR 验证读/写/覆盖/合并均正常，未动真实库）。`server.js` 静态服务每次读盘，前端改动无需重启。

**文件**：`js/schedule.js`（`loadSlots`/`saveSlots` 改 async + 走 meta）、`index.html`（版本号 `2026081109` → `2026081110`）

**验证**：`node --check js/schedule.js` 通过；版本号 9 处引用全更新到 `2026081110`；隔离测试确认 meta.slots 读写正确。

**注意**：
- 导出/导入备份时 meta 被 `SKIP_IMPORT` 跳过（不覆盖账号/设置），时段配置不会随备份迁移——符合"配置非业务数据"的设定；如需随备份带走，再议。
- 教室展示端 `classroom.js` 不维护时段配置，跨账号同步在 office 账号及同服务器登录者间生效。

