{
  "name": "kr-teacher-server",
  "version": "2.0.0",
  "description": "科任教师工作台 — SQLite 后端服务（零外部依赖，使用 Node.js 22 内置 node:sqlite）",
  "main": "server.js",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "private": true
}
