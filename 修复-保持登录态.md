# 修复说明：刷新页面后保持登录状态

## 问题现象
打开「科任教师工作台」网页，正常登录进入后，只要刷新浏览器（或关闭重开页面），就会回到登录界面，需要重新输入账号口令。

## 根本原因
后端其实**已经把登录令牌（token）持久化到了 SQLite 的 `sessions` 表**，刷新、甚至重启服务都不会失效；前端 `api.js` 也确实把 token 存进了浏览器的 `localStorage`。

问题出在前端启动逻辑 `js/app.js` 的 `initGate()`：**它读取了本地保存的 token，却每次都直接渲染登录页，没有用这个 token 去后端恢复会话**。所以刷新后虽然 token 还在，但页面直接显示登录框，表现为「退出登录」。

## 改动清单（4 个文件）
1. `server/database.js`：新增 `getAccountById(id)`，并按需导出。
2. `server/api.js`：新增接口 `GET /api/auth/me`（需登录），从令牌中解析并返回当前账号。
3. `js/api.js`：新增 `DB.me()` 方法（请求 `/api/auth/me`），对外暴露。
4. `js/app.js`：重写 `initGate()`——启动时若本地存在有效 token，先调用 `DB.me()` 自动恢复会话并进入应用；token 失效才回退到登录页。

## 验证结果
- `node --check` 全部语法通过。
- 端到端测试（临时库，未污染你的数据）：
  - 注册后正确返回 token ✅
  - 带 token 请求 `/api/auth/me` → `200`，正确返回当前账号 ✅
  - 不带 token 请求 → `401`（前端据此判定失效、显示登录页）✅

## 使用方式
1. **重启服务**让后端改动生效（停止后再 `start.bat` / `start.sh` 启动）。
2. 之后在浏览器中登录一次，刷新页面或关闭重开都会**自动保持登录**，不再跳回登录页。

## 注意事项
- 令牌长期有效（存在服务端数据库）。在**公共电脑**上用完后，务必点击左侧「退出」，会同时清除本地令牌与服务端会话。

---

## 补充修复（2026-08-11）：点击「退出」后退不出去

### 问题现象
上一步「保持登录」上线后，发现反方向的问题：点击左侧「退出」按钮后，页面一刷新又自动登录回去了，看起来"无法退出"。

### 根本原因
为了让刷新能保持登录，我改写了 `initGate()` 用本地 token 自动恢复会话。**但退出流程（`doLogout`）当初只做了"隐藏主界面 + 刷新页面"，漏掉了真正清除令牌的一步**——没有调用 `DB.logout()`。于是刷新后 `initGate()` 用残留的 token 又把会话恢复了，"退出"形同虚设。

> 换句话说：不是"写死"，而是退出动作漏了清令牌，而新增的自动恢复逻辑又把残留令牌立刻兑现了。

### 改动清单（1 个文件）
- `js/app.js`：`doLogout()` 改为 `async`，在 `location.reload()` 前先 `await DB.logout()`。
  - `DB.logout()` 会：① 调后端 `POST /api/auth/logout` 删除服务端会话；② `clearSession()` 清除 `localStorage` 中的令牌。
  - 重载后 `initGate()` 取不到有效令牌 → 自动显示登录页。

### 验证结果
- `node --check js/app.js` 语法通过。
- 服务返回的前端已包含 `await DB.logout`（版本号升至 `2026081102`）。

### 浏览器端必需一步
由于浏览器缓存了旧的前端 JS，请**硬刷新一次**（`Ctrl+F5` / `Cmd+Shift+R`）让新版本号生效，之后退出才会真正生效。

### 测试方式
1. 硬刷新页面（`Ctrl+F5`）。
2. 登录账号。
3. 点击左侧「退出」→ 确认框点「退出」。
4. 退出后应停在登录页；再刷新一次也应**保持登录页**（需重新输入账号口令），说明令牌已清除。

---

## 补充修复（2026-08-11 后续）：教室账号点击「退出」仍自动登录

### 问题现象
办公室账号的退出修复后，发现**教室展示账号**登录后点「退出」再刷新，又自动回到教室界面 —— 退不出去。

### 根本原因
教室端（`js/classroom.js`）有自己独立的退出按钮 `#cbExit`，其点击事件当初同样是**只 `location.reload()`、漏掉了 `DB.logout()`**。刷新后 `initGate()` 用残留 token 又把教室会话恢复了。上一轮只改了办公室端的 `doLogout()`，没覆盖教室端这条独立路径。

### 改动清单（1 个文件）
- `js/classroom.js`：`#cbExit` 的点击事件改为 —— 确认后先 `await DB.logout()`（清 localStorage + 删服务端 session），再 `location.reload()`；重载后 `initGate()` 取不到有效令牌 → 显示登录页。

### 配套
前端版本号 `2026081102` → `2026081103`，强制浏览器重新加载 classroom.js。后端无需重启（静态文件直接读盘）。

### 验证结果
- `node --check js/classroom.js` 语法通过。
- 服务返回的 `classroom.js` 已包含 `await DB.logout`。

### 测试方式（教室账号）
1. 硬刷新页面（`Ctrl+F5`）让新版本号生效。
2. 用教室展示账号登录。
3. 点左侧「退出」→ 确认「退出」。
4. 应回到登录页；再刷新仍为登录页（需重输账号），说明令牌已清除 ✓

---

## 补充修复（2026-08-11 后续）：登录令牌改为 2 小时有效期（不再永久）

### 需求
原来登录令牌（token）永久有效（存在服务端 `sessions` 表、无过期时间）。用户要求改为**有效期 2 小时**。

### 改动清单（后端 1 个文件 + 前端 2 个文件）
- `server/database.js`：
  - `sessions` 表新增 `expire_at` 字段（建表时带列 + `ALTER TABLE` 兼容旧库）。
  - 新增常量 `TOKEN_TTL_MS = 2 * 60 * 60 * 1000`（2 小时）。
  - `createSession()`：写入 `expire_at = now + 2h`。
  - `getSession()`：若 `expire_at <= now` 则删除该会话并返回 `null`（→ 后端返回 401）。
  - 服务启动时把旧库里 `expire_at IS NULL` 的历史会话统一补为 `now + 2h`，避免已登录会话立即掉线、也使其不再永久。
- `js/api.js`：`request()` 遇到 401 时派发 `kr:session-expired` 事件（已 `clearSession`）。
- `js/app.js`：监听 `kr:session-expired`，触发 `forceLoginGate()` 自动回到登录页（不刷新、避免死循环）。
- `index.html`：前端版本号 `2026081103` → `2026081104`，强制重新加载前端。

### 验证结果
- 语法：`node --check` 全部通过。
- 临时库端到端（TTL 临时改为 1.5 秒以便测试）：登录拿 token → 立即请求 `/api/auth/me` 返回 `200`；等待超过有效期后请求返回 `401` ✓。
- 真实库核对：`sessions` 表中 28 个历史会话的 `expire_at` 全部被补为未来时间（约 2 小时后过期），不再永久有效 ✓。

### 行为说明
- 令牌自**登录/创建时刻**起 2 小时内有效；超过 2 小时后再访问会收到 401，前端自动回到登录页，需重新登录。
- 当前为**固定 2 小时**（非滑动续期）：即使用途中频繁操作，到点也会过期。若希望"活跃使用期间不过期、仅空闲超 2 小时才过期"，可改为滑动续期（每次校验成功时把 `expire_at` 刷新为 `now + 2h`），如需我可再调整。
- 退出登录（`DB.logout()`）会立即删除服务端会话并清除本地令牌，不受 2 小时限制。

